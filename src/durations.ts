export const enum DurationS {
    Second = 1,
    Minute = 60,
    Hour = 60 * Minute,
    Day = 24 * Hour,
    Month = 30 * Day,
}

export const enum DurationMs {
    Second = 1000,
    Minute = 60 * Second,
    Hour = 60 * Minute,
    Day = 24 * Hour,
    Month = 30 * Day,
}
